<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Gerador de Currículo</title>
    <style>
        body {
  font-family: Arial, sans-serif;
  background-color: #f4f4f4;
  margin: 0;
  padding: 20px;
}

h1 {
  text-align: center;
  color: #333;
}

form {
  max-width: 600px;
  margin: 20px auto;
  background: white;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

label {
  font-weight: bold;
  display: block;
  margin-top: 15px;
  color: #555;
}

input, textarea, select {
  width: 100%;
  padding: 10px;
  margin-top: 5px;
  border: 1px solid #ccc;
  border-radius: 5px;
  font-size: 14px;
  box-sizing: border-box;
}

textarea {
  resize: vertical;
  min-height: 80px;
}

button {
  display: block;
  margin: 20px auto 0;
  background: #007BFF;
  color: white;
  border: none;
  padding: 12px 20px;
  font-size: 16px;
  border-radius: 5px;
  cursor: pointer;
}

button:hover {
  background: #0056b3;
}

#resultado {
  max-width: 700px;
  margin: 40px auto;
}

/* Currículo */
#curriculo {
  font-family: Arial, sans-serif;
  padding: 20px;
  max-width: 700px;
  background: white;
  border-radius: 10px;
  box-shadow: 0 0 15px rgba(0,0,0,0.1);
}

/* Cabeçalho com nome e foto lado a lado */
#curriculo .cabecalho {
  display: flex;
  align-items: center;
  gap: 20px;
}

  #curriculo .foto {
  width: 100px;  /* 3 cm */
  height: 130px;   /* 4.0 cm */
  border-radius: 4px;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #ffff; /* opcional, pra mostrar fundo se não tiver foto */
}

#curriculo .foto img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;

}

#curriculo h2 {
  margin: 0;
  color: #007BFF;
}

/* Contatos lado a lado */
#curriculo .contatos {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  font-size: 14px;
  color: #333;
  margin-top: 5px;
}

#curriculo .contatos p {
  margin: 0;
  min-width: 150px;
}

#curriculo p {
  font-size: 14px;
  margin: 5px 0;
  color: #333;
}

#curriculo h3 {
  border-bottom: 2px solid #007BFF;
  padding-bottom: 5px;
  margin-top: 20px;
  color: #007BFF;
}

#curriculo .btn-excluir{
  display: none;
}

/* Estilo para experiências dentro do formulário */
#experiencias-container {
  position: relative;
}

.experiencia{
  position: relative;
  margin: 8px 0;
}

.experiencia-item {
  background: #e9ecef;
  padding: 0 10px;
  margin-bottom: 10px;
  border-radius: 6px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: relative;
}

.experiencia-item label {
  font-weight: bold;
  display: block;
  margin-top: 8px;
}

.experiencia-item input {
  width: 100%;
  padding: 8px;
  margin-top: 3px;
  border-radius: 4px;
  border: 1px solid #ccc;
  box-sizing: border-box;
}

input[type="radio"] {
  width: auto;
  visibility: hidden;
}

.color {
  width: 20px;
  height: 20px;
  display: inline-block;
  max-width: 20px;
  border-radius: 4px;
  padding: 5px;
  cursor: pointer;
}

.c-cinza{
  background-color: #45484A;
}
.c-azul{
  background-color: #007BFF;
}
.c-rosa{
  background-color: #e39bb4;
}
.c-verde{
  background-color: #064c41;
}


#baixarPDF {
  display: block;
  margin: 20px auto;
  background: #28a745;
  color: white;
  border: none;
  padding: 12px 20px;
  font-size: 16px;
  border-radius: 5px;
  cursor: pointer;
}

#baixarPDF:hover {
  background: #1e7e34;
}

.modal {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  justify-content: center;
  align-items: center;
}

.modal-content {
  background: white;
  padding: 20px;
  border-radius: 8px;
  width: 300px;
}
.cabecalho {
    display: flex;
    gap: 15px; /* espaço entre foto e texto */
    align-items: flex-start;
}

.cabecalho .foto {
    width: 90px;      /* 3 */
    height: 120px;    /* x4 proporção */
    overflow: hidden;
    border-radius: 4px;
    flex-shrink: 0; /* impede de diminuir */
}

.cabecalho .foto img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* corta o excesso */
    object-position: center; /* centraliza o rosto */
}
#curriculo.template1 {
  font-family: Arial, sans-serif;
  padding: 20px;
  max-width: 700px;
  background: white;
  border-radius: 10px;
  box-shadow: 0 0 15px rgba(0,0,0,0.1);
}

#curriculo.template1 .cabecalho {
  display: flex;
  align-items: center;
  gap: 20px;
}

#curriculo.template1 .foto {
  width: 100px;
  height: 130px;
  border-radius: 4px;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #fff;
}

#curriculo.template1 .foto img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

#curriculo.template1 h2 {
  margin: 0 0 25px 0;
  color: #45484A;
}

#curriculo.template1 .contatos {
  display: flex;
  flex-wrap: wrap;
  gap: 15px;
  font-size: 14px;
  color: #333;
  margin-top: 5px;
  line-height: 50%;
}

#curriculo.template1 .contatos p {
  margin: 0;
  min-width: 150px;
}

#curriculo.template1 p {
  font-size: 14px;
  margin: 5px 0;
  color: #333;
}

#curriculo.template1 h3 {
  border-bottom: 2px solid #45484A;
  padding-bottom: 5px;
  margin-top: 20px;
  color: #45484A;
}
.template2 {
  display: flex;
  background: white;
  border-radius: 10px;
  box-shadow: 0 0 15px rgba(0,0,0,0.1);
  overflow: hidden;
  font-family: Arial, sans-serif;
}

.template2 .sidebar {
  background: #007BFF; /* azul */
  color: white;
  padding: 20px;
  width: 250px;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  font-weight: 600;
}

.template2 .sidebar .foto img {
  width: 120px;
  height: 160px;
  border-radius: 10px;
  object-fit: cover;
  display: block;
}

.template2 main {
  flex: 1;
  padding: 20px 30px;
  color: #333;
}
.template2 .sidebar .foto {
  width: 100px;
  height: 130px;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
  background: #fff;
  border-radius: 4px;
}
/* ===== Ajuste universal para fotos nos templates ===== */
.foto-container {
  width: 100px;
  height: 130px;
  overflow: hidden;
  border-radius: 6px;
}

.foto-bg {
  width: 100%;
  height: 100%;
  background-size: cover;      /* mantém proporção */
  background-position: center; /* centraliza */

}
.template1 .foto-container {
  width: 200px;
  height: 130px;
  overflow: hidden;
  border-radius: 6px;
}

.template1 .foto-bg{
      background-size: cover;      /* mantém proporção */
  background-position: center; /* centraliza */
}

/* Se algum template tiver tamanho de foto diferente, personalize assim: */
.template3 .foto-container {
  width: 110px;
  height: 140px;
}

.template5 .foto-container {
  width: 90px;
  height: 120px;
}

.template2 .sidebar {
  background: #007BFF; /* azul */
  padding: 20px;
  width: 250px;
  box-sizing: border-box;
  flex-direction: column;
  align-items: left;
  font-weight: 600;
  /* Adicionado para garantir texto branco */
  color: white !important;
}

.template2 .sidebar * {
  color: white !important;  /* força branco em todos os filhos */
}

.template2 .sidebar h2.nome-barra {
  margin-top: 0;
  margin-bottom: 20px;
  font-size: 1.8rem;
  font-weight: 700;
  text-align: center;
}

/* Cor do nome principal no topo da área principal */
.template2 .nome-principal {
  font-size: 2rem;
  font-weight: 900;
  margin-top: 0;
  margin-bottom: 20px;
  color: #007BFF;
}

/* Template 3 - Cabeçalho colorido */
.template3 {
  background: white;
  border-radius: 10px;
  box-shadow: 0 0 15px rgba(0,0,0,0.1);
  padding: 20px 30px;
}

.template3 header {
  background: #007BFF;
  color: white;
  display: flex;
  align-items: center;
  padding: 15px 25px;
  border-radius: 8px 8px 0 0;
  gap: 20px;
}

.template3 header .foto-cabecalho img {
  width: 90px;
  height: 120px;
  object-fit: cover;
  border-radius: 6px;
}

.template3 header .info-cabecalho h2 {
  margin: 0;
  font-size: 2rem;
  font-weight: bold;
}

.template3 header .info-cabecalho p {
  margin: 4px 0;
  font-size: 1rem;
  color: #fff!important;
}
/* Cor do nome principal no topo da área principal */
.template3 .nome-principal {
  font-size: 2rem;
  font-weight: 900;
  margin-top: 0;
  margin-bottom: 20px;
  color: #ffffff !important;
  background-color: #0606062e;
  padding: 5px;
  border-radius: 10px;
}
#curriculo.template4 {
  max-width: 700px;
  background: white;
  padding: 25px;
  margin: 20px auto;
  font-family: Arial, sans-serif;
  border: 2px solid #007BFF;
  border-radius: 10px;
  box-shadow: 0 0 10px rgba(0,0,0,0.2);
}

#curriculo.template4 .cabecalho {
  display: flex;
  gap: 15px;
  align-items: center;
  margin-bottom: 20px;
}

#curriculo.template4 .foto {
  width: 90px;
  height: 120px;
  overflow: hidden;
  border-radius: 6px;
  background: #f0f0f0;
  flex-shrink: 0;
}

#curriculo.template4 .foto img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

#curriculo.template4 h2 {
  margin: 0 0 8px 0;
  color: #007BFF;
}

#curriculo.template4 p {
  font-size: 14px;
  margin: 6px 0;
}

#curriculo.template4 h3 {
  border-bottom: 2px solid #007BFF;
  padding-bottom: 6px;
  margin-top: 25px;
  font-weight: 600;
  color: #007BFF;
}
/* Template 5 - Corporativo */
.template5 {
  background: white;
  border-radius: 10px;
  box-shadow: 0 0 20px rgba(0,0,0,0.15);
  padding: 25px 35px;
  color: #222;
}

.template5 header {
  display: flex;
  align-items: center;
  gap: 20px;
  margin-bottom: 25px;
}

.template5 header .foto-corporativo img {
  width: 100px;
  height: 140px;
  object-fit: cover;
  border-radius: 6px;
  flex-shrink: 0;
}

.template5 header .info-corporativo h1 {
  margin: 0 0 10px 0;
  font-size: 2.5rem;
  font-weight: 900;
  color: #004080;
}

.template5 header .info-corporativo p {
  margin: 4px 0;
  font-size: 1rem;
  color: #444;
}

#button-lixo {
  margin: auto;
  background-color: #f3f2f7;
  padding: 3px;
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
}

#listaExperiencias {
  margin: 20px;
}
#button-lixo :hover {
  background-color: #f3f2f7;
}
    </style>
</head>
<body>
    <h1>Gerador de Currículo</h1>

    <form id="curriculoForm">
        <label>Foto (3x4 cm):</label>
        <input type="file" id="foto" accept="image/*" />

        <label>Nome Completo:</label>
        <input type="text" id="nome" required />
        
        <label>Data de nascimento:</label>
        <input type="date" id="nascimento" required />
        
        <label>Nacionalidade</label>
        <input type="text" id="nacionalidade" />
        
        <label>Telefone:</label>
        <input type="tel" id="telefone" required />
        
        <label>Email:</label>
        <input type="email" id="email" />

        <label>Estado civil:</label>
        <select id="estadoCivil" required>
            <option value="">Selecione</option>
            <option value="Solteiro(a)">Solteiro(a)</option>
            <option value="Casado(a)">Casado(a)</option>
            <option value="Divorciado(a)">Divorciado(a)</option>
            <option value="Viúvo(a)">Viúvo(a)</option>
        </select>

        <label>Endereço:</label>
        <input type="text" id="endereco" required />

        <label>Objetivo Profissional:</label>
        <textarea id="objetivo" required value="Pretendo contribuir com dedicação e comprometimento, visando alcançar, junto à empresa, resultados de excelência."></textarea>

        <label>Experiência Profissional:</label>
                <!-- Botão para abrir o modal -->
        <button type="button" id="btnAddExperiencia">Adicionar Experiência</button>

        <!-- Container onde as experiências serão listadas -->
        <div id="listaExperiencias"></div>

        <!-- Modal (popup) para adicionar experiência -->
        <div id="modalExperiencia" class="modal" style="display:none;">
        <div class="modal-content">
            <h3>Adicionar Experiência</h3>
            <label>Empresa:</label>
            <input type="text" id="inputEmpresa" />

            <label>Cargo:</label>
            <input type="text" id="inputCargo" />

            <label>Período:</label>
            <input type="text" id="inputPeriodo" />

            <button id="btnSalvarExperiencia">Salvar</button>
            <button id="btnCancelarExperiencia">Cancelar</button>
        </div>
        </div>

        <label>Formação Acadêmica:</label>
        <textarea id="formacao" required></textarea>

        <label>Cursos:</label>
        <textarea id="habilidades"></textarea>


        <label>Escolha uma cor:</label>
        <div style="display: flex;">
            <label for="cinza" class="color c-cinza"></label>
            <input type="radio" name="color" id="cinza" value="#45484A">
            <label for="azul" class="color c-azul"></label>
            <input type="radio" name="color" id="azul" value="#007BFF">
            <label for="rosa" class="color c-rosa"></label>
            <input type="radio" name="color" id="rosa" value="#e39bb4">
            <label for="verde" class="color  c-verde"></label>
            <input type="radio" name="color" id="verde" value="#064c41">
        </div>
        
        <label>Escolher Modelo:</label>
        <select id="templateSelecionado">
            <option value="1">Template 1 - Minimalista</option>
            <option value="2">Template 2 - Barra Lateral</option>
            <option value="3">Template 3 - Cabeçalho Colorido</option>
            <option value="4">Template 4 - Bordas Elegantes</option>
        </select>
        
        <button type="submit">Gerar Currículo</button>

    </form>

    <div id="resultado"></div>

    <!-- Bibliotecas para gerar PDF -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <script>
        const { jsPDF } = window.jspdf;

window.addEventListener("DOMContentLoaded", () => {
  const objetivoTextarea = document.getElementById("objetivo");
  if (objetivoTextarea && !objetivoTextarea.value) {
    objetivoTextarea.value = "Pretendo contribuir com dedicação e comprometimento, visando alcançar, junto à empresa, resultados de excelência.";
  }
});


document.getElementById("curriculoForm").addEventListener("submit", function(event) {
    event.preventDefault();

    let nome = document.getElementById("nome").value;
    let email = document.getElementById("email").value;
    let nacionalidade = document.getElementById("nacionalidade").value;
    let telefone = document.getElementById("telefone").value;
    let nascimento = document.getElementById("nascimento").value;
    let estadoCivil = document.getElementById("estadoCivil").value;
    let endereco = document.getElementById("endereco").value;
    let objetivo = document.getElementById("objetivo").value;
    let formacao = document.getElementById("formacao").value;
    let habilidades = document.getElementById("habilidades").value;
    let foto = document.getElementById("foto").files[0];
    let cor = document.querySelector('input[name="color"]:checked').value;
    let template = document.getElementById("templateSelecionado").value;

    if (nascimento) {
        const dataObj = new Date(nascimento);
        nascimento = dataObj.toLocaleDateString("pt-BR", { timeZone: "UTC" });
    }

    telefone = telefone.replace(/\D/g, "");
    if (telefone.length === 11) {
        telefone = telefone.replace(/(\d{2})(\d{1})(\d{4})(\d{4})/, "($1) $2 $3-$4");
    }

    function montarExperiencias() {
        let experiencias = "";
        const lista = document.querySelectorAll(".experiencia-item");
        lista.forEach(item => {
            experiencias += `<p>${item.innerHTML}</p>`;
        });
        return experiencias || "<p>Em Busca do meu Primeiro emprego</p>";
    }

    function gerarCurriculoTemplate(template, fotoBase64) {
        switch(template) {
            case "1":
                return `
                <div id="curriculo" class="template1">
                  <div class="cabecalho">
                    <div>
                      <h2 style="color: ${cor}">${nome}</h2>
                      <div class="contatos">
                        <p>${nacionalidade}, ${estadoCivil}, Nascimento: ${nascimento}</p>
                        <p>Endereço: ${endereco}</p>
                        <p>Telefone: ${telefone}</p>
                        <p>${email}</p>
                      </div>
                    </div>
                    <div class="foto-container">
                      ${fotoBase64 ? `<div class="foto-bg" style="background-image: url('${fotoBase64}');"></div>` : ""}
                    </div>
                  </div>

                  <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Objetivo Profissional</h3>
                  <p>${objetivo}</p>

                  <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Formação Acadêmica</h3>
                  <p>${formacao}</p>
                  
                  <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">${habilidades ? 'Cursos': ''}</h3>
                  <p>${habilidades}</p>
                  
                  <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Experiência Profissional</h3>
                  ${montarExperiencias()}
                </div>
                `;

            case "2":
                return `
                <div id="curriculo" class="template2"> 
                  <aside class="sidebar" style="background-color: ${cor}">
                    <div class="foto-container">
                      ${fotoBase64 ? `<div class="foto-bg" style="background-image: url('${fotoBase64}');"></div>` : ""}
                    </div>
                    <p><strong>Email:</strong> ${email}</p>
                    <p><strong>Telefone:</strong> ${telefone}</p>
                    <p><strong>Data de nascimento:</strong> ${nascimento}</p>
                    <p><strong>Estado civil:</strong> ${estadoCivil}</p>
                    <p><strong>Endereço:</strong> ${endereco}</p>
                  </aside>
                  <main>
                    <h2 class="nome-principal" style="color: ${cor}">${nome}</h2>
                    <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Objetivo Profissional</h3>
                    <p>${objetivo}</p>
                    
                    <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Formação Acadêmica</h3>
                    <p>${formacao}</p>
                    
                    <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Cursos</h3>
                    <p>${habilidades}</p>
                    
                    <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Experiência Profissional</h3>
                    ${montarExperiencias()}
                  </main>
                </div>
                `;

            case "3":
                return `
                <div id="curriculo" class="template3">
                    <header style="background: ${cor}">
                        <div class="foto-container">
                          ${fotoBase64 ? `<div class="foto-bg" style="background-image: url('${fotoBase64}');"></div>` : ""}
                        </div>
                        <div class="info-cabecalho">
                            <h2 class="nome-principal">${nome}</h2>
                            <p><strong>Email:</strong> ${email}</p>
                            <p><strong>Telefone:</strong> ${telefone}</p>
                            <p><strong>Data de nascimento:</strong> ${nascimento}</p>
                            <p><strong>Estado civil:</strong> ${estadoCivil}</p>
                            <p><strong>Endereço:</strong> ${endereco}</p>
                        </div>
                    </header>

                    <section>
                        <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Objetivo Profissional</h3>
                        <p>${objetivo}</p>
                    </section>
                    
                    <section>
                        <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Formação Acadêmica</h3>
                        <p>${formacao}</p>
                    </section>
                    
                    <section>
                        <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Cursos</h3>
                        <p>${habilidades}</p>
                    </section>

                    <section>
                        <h3 style="color: ${cor};border-bottom: 2px solid ${cor};">Experiência Profissional</h3>
                        ${montarExperiencias()}
                    </section>
                </div>
                `;

            case "4":
                return `
                <div id="curriculo" class="template5">
                  <header>
                    <div class="foto-container">
                      ${fotoBase64 ? `<div class="foto-bg" style="background-image: url('${fotoBase64}');"></div>` : ""}
                    </div>
                    <div class="info-corporativo">
                      <h2>${nome}</h2>
                      <p><strong>Email:</strong> ${email} | <strong>Telefone:</strong> ${telefone} | <strong>Estado civil:</strong> ${estadoCivil}</p>
                      <p><strong>Data de nascimento:</strong> ${nascimento} | <strong>Endereço:</strong> ${endereco}</p>
                    </div>
                  </header>

                  <section>
                    <h2>Objetivo Profissional</h2>
                    <p>${objetivo}</p>
                  </section>

                  <section>
                    <h2>Experiência Profissional</h2>
                    ${montarExperiencias()}
                  </section>

                  <section>
                    <h2>Formação Acadêmica</h2>
                    <p>${formacao}</p>
                  </section>

                  <section>
                    <h2>Habilidades</h2>
                    <p>${habilidades}</p>
                  </section>
                </div>
                `;
        }
    }

    function gerarCurriculo(fotoBase64) {
        const curriculoHTML = gerarCurriculoTemplate(template, fotoBase64);
        const resultadoHTML = curriculoHTML + `<button id="baixarPDF">📄 Baixar em PDF</button>`;
        document.getElementById("resultado").innerHTML = resultadoHTML;

        const botaoPDF = document.getElementById("baixarPDF");
        botaoPDF.addEventListener("click", async function() {
            const elemento = document.getElementById("curriculo");

            const botoes = elemento.querySelectorAll("button");
            botoes.forEach(btn => btn.style.display = "none");

            const imgs = elemento.querySelectorAll("img");
            await Promise.all(Array.from(imgs).map(img => {
                if (img.complete) return;
                return new Promise(resolve => {
                    img.onload = img.onerror = resolve;
                });
            }));

            const canvas = await html2canvas(elemento, {
                scale: 4,
                useCORS: true,
                backgroundColor: "#ffffff"
            });

            const imgData = canvas.toDataURL("image/jpeg", 1.0);
            const pdf = new jsPDF("p", "mm", "a4");

            const imgProps = pdf.getImageProperties(imgData);
            const pdfWidth = pdf.internal.pageSize.getWidth();
            const pdfHeight = (imgProps.height * pdfWidth) / imgProps.width;

            pdf.addImage(imgData, "JPEG", 0, 0, pdfWidth, pdfHeight);
            pdf.save(`${nome}_curriculo.pdf`);

            botoes.forEach(btn => btn.style.display = "");
        });
    }

    if (foto) {
        let reader = new FileReader();
        reader.onload = function(e) {
            gerarCurriculo(e.target.result);
        };
        reader.readAsDataURL(foto);
    } else {
        gerarCurriculo(null);
    }
});

const btnAddExperiencia = document.getElementById("btnAddExperiencia");
const modalExperiencia = document.getElementById("modalExperiencia");
const btnSalvarExperiencia = document.getElementById("btnSalvarExperiencia");
const btnCancelarExperiencia = document.getElementById("btnCancelarExperiencia");

const inputEmpresa = document.getElementById("inputEmpresa");
const inputCargo = document.getElementById("inputCargo");
const inputPeriodo = document.getElementById("inputPeriodo");

const listaExperiencias = document.getElementById("listaExperiencias");

btnAddExperiencia.addEventListener("click", () => {
  inputEmpresa.value = "";
  inputCargo.value = "";
  inputPeriodo.value = "";
  modalExperiencia.style.display = "flex";
  inputEmpresa.focus();
});

btnCancelarExperiencia.addEventListener("click", () => {
  modalExperiencia.style.display = "none";
});

btnSalvarExperiencia.addEventListener("click", () => {
  const empresa = inputEmpresa.value.trim();
  const cargo = inputCargo.value.trim();
  const periodo = inputPeriodo.value.trim();

  if (!empresa || !cargo || !periodo) {
    alert("Por favor, preencha todos os campos.");
    return;
  }

  const experienciaDiv = document.createElement("div");
  experienciaDiv.classList.add("experiencia-item");
  experienciaDiv.innerHTML = `
    <div class="experiencia">
      <strong>Empresa:</strong> ${empresa} <br>
      <strong>Cargo:</strong> ${cargo} <br>
      <strong>Período:</strong> ${periodo}
    </div>
    <button class="btn-excluir" id="button-lixo">⛔</button>
  `;

  experienciaDiv.querySelector(".btn-excluir").addEventListener("click", function() {
    experienciaDiv.remove();
  });

  listaExperiencias.appendChild(experienciaDiv);
  modalExperiencia.style.display = "none";
});

    </script>
</body>
</html>
